name: 01 Frontend workflow

env:
  dir_name: 'frontend'

on:
  workflow_dispatch:
  push:
    branches:
      - cicd
    paths:
      - 'frontend/**'

jobs:
  debug:
    uses: ./.github/workflows/debug.yaml
     

  # secret-scan:
  #   uses: ./.github/workflows/secrets-scan.yaml
  #   with:
  #     branch_name: 'cicd'
  #     dir_name: 'frontend'