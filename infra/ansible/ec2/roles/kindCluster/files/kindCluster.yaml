kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
    image: kindest/node:v1.34.0
    extraPortMappings:
      - containerPort: 30010
        hostPort: 80

  # - role: worker
  #   image: kindest/node:v1.34.0

# kind create cluster --config kindCluster.yaml --name ecom-cluster
# kind delete cluster --name ecom-cluster
# kind get clusters
# kind load docker-image hackerspider09/ecom-frontend:latest --name ecom-cluster
# docker exec -it kind-cluster-control-plane crictl images 