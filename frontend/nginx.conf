server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA Fallback: serve index.html for all routes so React Router can handle them
    location / {
        try_files $uri $uri/ /index.html;
    }
}
